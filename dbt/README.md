# dbt project skeleton

Learning goals:
- Initialize a dbt project pointed at DuckDB or Postgres.
- Define models for your portfolio tables.

Next steps:
- Install dbt (e.g. dbt-duckdb or dbt-postgres).
- Configure your `profiles.yml` (usually in ~/.dbt/profiles.yml) to point at the right database.
